{
  "name": "server-template",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "predeploy": "sam build",
    "prestart": "sam build",
    "start": "sam local start-api",
    "build": "sam build",
    "deploy": "sam deploy"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "npm-run-all2": "^6.2.0"
  },
  "dependencies": {
    "hono": "^4.6.1"
  }
}
