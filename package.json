{
  "name": "server-template",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "predeploy": "sam build",
    "prestart": "sam build",
    "start": "sam local start-api",
    "build": "sam build",
    "deploy": "sam deploy"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "npm-run-all2": "^6.2.0",
    "aws-sdk": "^2.1691.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "hono": "^4.6.1",
    "mongoose": "^8.6.2"
  }
}
